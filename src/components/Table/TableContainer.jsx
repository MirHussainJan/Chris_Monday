const TableContainer = ({ children }) => {
  return (
    <section
      style={{
        // display: "flex",
        // justifyContent: "center",
        marginTop: "12px",
      }}
    >
      {children}
    </section>
  );
};

export default TableContainer;
